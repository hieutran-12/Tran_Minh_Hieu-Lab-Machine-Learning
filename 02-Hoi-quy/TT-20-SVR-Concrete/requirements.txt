# TT-20 — SVR Concrete Compressive Strength
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3
xlrd>=2.0            # đọc Concrete_Data.xls của UCI
openpyxl>=3.1        # đọc .xlsx nếu dùng bản chuyển đổi
xgboost>=2.0         # bước 11 — so sánh model (có thể bỏ, code tự bỏ qua)
jupyter>=1.0         # chạy notebooks/svr_concrete.ipynb
